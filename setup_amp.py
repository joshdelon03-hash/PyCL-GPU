from setuptools import setup, find_packages

setup(
    name="amp",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="Adaptive Multiprocessing Pool for Python",
)
